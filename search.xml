<?xml version="1.0" encoding="utf-8"?>
<search>
  <entry>
    <title>Cpp基础知识</title>
    <url>/2021/08/06/C-%E5%9F%BA%E7%A1%80%E7%9F%A5%E8%AF%86/</url>
    <content><![CDATA[<p><strong>介绍：</strong>仅作个人笔记，侵删，通过①  <a href="https://light-city.club/sc">https://light-city.club/sc</a> ② 《C++primer》等途径复习及完全掌握C++，本文包含以下内容：</p>
<ol>
<li>类型修饰符const</li>
</ol>
<span id="more"></span>

<h2 id="const"><a href="#const" class="headerlink" title="const"></a>const</h2><p><strong>原则：const离谁近，谁就不能被修改</strong></p>
<h4 id="1-作用"><a href="#1-作用" class="headerlink" title="1 作用"></a>1 作用</h4><p><strong>（1）定义常量。</strong></p>
<p><strong>需要注意几个问题：</strong></p>
<p><strong>① 与C语言的区别：C值可改，C++不可改</strong></p>
<p>  需要注意的是在C语言中，const定义的常量可以通过指针修改，但是C++不行。</p>
<p>如下：</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="meta-keyword">include</span><span class="meta-string">&lt;stdio.h&gt;</span></span></span><br><span class="line"> </span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">main</span><span class="params">()</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="keyword">const</span> <span class="keyword">int</span> a = <span class="number">5</span>;</span><br><span class="line">    <span class="keyword">int</span> *p = &amp;a;</span><br><span class="line">    *p = <span class="number">1</span>;</span><br><span class="line">    <span class="built_in">printf</span>(<span class="string">&quot;a = %d&quot;</span>,a) </span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>输出：a = 1</p>
<p>如果上面这段代码出现在C++中那么编译器会报错：<strong>试图将 const int* 型赋给 int * 型</strong>，</p>
<p>​    若改正，则只需将 int *p 改为 const int *p (<strong>意思为(*p)的值不能改变</strong>(指针p的值可以改变）<strong>与 int * const p 有区别</strong>）</p>
<p><strong>② 使用const定义常量时，必须初始化（因为定义后不能被修改，所以定义时必须初始化）</strong></p>
<p><strong>（2）类型检查</strong></p>
<p>const常量与#define宏定义常量的区别与联系：</p>
<ol>
<li><p>安全检查不同</p>
<p>define的宏定义也存在类型，但是不做任何检查，仅仅是字符替换，并且在字符替换时可能会产生意想不到的错误；而const常量有明确的类型，在编译阶段会进行类型检查。</p>
</li>
<li><p>处理阶段不同</p>
<p>define是在预处理阶段，define常量从未被编译器看见，因为在预处理截断就已经替换了；</p>
</li>
</ol>
<p>　　const常量在编译阶段使用</p>
<ol start="3">
<li><p>存储方式不同</p>
<p>define是字符替换，有多少地方使用，就会替换多少次，在内存中有若干个拷贝；</p>
<p>编译器通常不会为const常量分配空间，只是将它们保存在符号表内，使他们成为一个编译期间的一个常量，没有读取内存的操作，效率也很高。</p>
</li>
</ol>
<p>⭐注意！！：<code>const</code> 定义的变量只有类型为整数或枚举，且以常量表达式初始化时才能作为常量表达式。其他情况下它只是一个 <code>const</code> 限定的变量，不要将与常量混淆。</p>
<p>​    </p>
<p><strong>（3）防止修改，起保护作用，增强程序健壮性</strong>。</p>
<p>​    比如用const修饰函数的参数，</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">func</span><span class="params">(<span class="keyword">const</span> <span class="keyword">int</span> i)</span></span>&#123;</span><br><span class="line">	i++;<span class="comment">//错误</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p><strong>（4）可以节省空间，避免不必要的内存分配。</strong></p>
<p>const定义常量从汇编的角度来看，只是给出了对应的内存地址，而不是像#define一样给出的是立即数，所以，const定义的常量在程序运行过程中只有一份拷贝，而#define定义的常量在内存中有若干个拷贝。</p>
<h4 id="2、-用法："><a href="#2、-用法：" class="headerlink" title="2、 用法："></a>2、 用法：</h4><p>​    </p>
<h5 id="1-修饰变量。"><a href="#1-修饰变量。" class="headerlink" title="1.修饰变量。"></a>1.修饰变量。</h5><p>​            </p>
]]></content>
      <categories>
        <category>Cpp基础</category>
      </categories>
      <tags>
        <tag>Cpp基础</tag>
      </tags>
  </entry>
  <entry>
    <title>剑指offer03</title>
    <url>/2021/08/06/%E5%89%91%E6%8C%87offer03/</url>
    <content><![CDATA[<p><strong>介绍：找出数组中重复的数字。</strong></p>
<p>在一个长度为 n 的数组 nums 里的所有数字都在 0～n-1 的范围内。数组中某些数字是重复的，但不知道有几个数字重复了，也不知道每个数字重复了几次。请找出数组中任意一个重复的数字。</p>
<p>示例 1：</p>
<p>输入：<br>[2, 3, 1, 0, 2, 5, 3]<br>输出：2 或 3 </p>
<p>限制：</p>
<p>2 &lt;= n &lt;= 100000</p>
<p>来源：力扣（LeetCode）<br>链接：<a href="https://leetcode-cn.com/problems/shu-zu-zhong-zhong-fu-de-shu-zi-lcof">https://leetcode-cn.com/problems/shu-zu-zhong-zhong-fu-de-shu-zi-lcof</a><br>著作权归领扣网络所有。商业转载请联系官方授权，非商业转载请注明出处。</p>
<span id="more"></span>

<h4 id="自己解法：排序比对"><a href="#自己解法：排序比对" class="headerlink" title="自己解法：排序比对"></a>自己解法：排序比对</h4><p>因为题目给的nums是vector，所以想到直接用sort函数，排序后比对</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">class Solution &#123;</span><br><span class="line">public:</span><br><span class="line">    int findRepeatNumber(vector&lt;int&gt;&amp; nums) &#123;</span><br><span class="line">        sort(nums.begin(), nums.end());</span><br><span class="line">        for (int i = 0; i &lt; (int)nums.size() - 1; i++) &#123;</span><br><span class="line">            if (nums[i] == nums[i + 1]) &#123;</span><br><span class="line">                return nums[i];</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">        return -1;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>

<p><img src="https://cdn.jsdelivr.net/gh/RDXSun/img_warehouse/img/20210806162347.png"></p>
<h4 id="sort函数解释"><a href="#sort函数解释" class="headerlink" title="sort函数解释"></a>sort函数解释</h4><p>sort函数包含在头文件为#include&lt; algorithm &gt; 的C++标准库中。</p>
<p>时间复杂度为n*log2(n)，类似于快速排序的方法。</p>
<p>参数：sort（start，end，cmp）</p>
<p>（1）若是数组：则可以sort(a, a+10)</p>
<p>（2）若是vector：sort(a.start(), a.end() )</p>
<p>（3）若想降序排列，可以</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">bool</span> <span class="title">compare</span><span class="params">(<span class="keyword">int</span> a, <span class="keyword">int</span> b)</span></span>&#123;</span><br><span class="line">	<span class="keyword">return</span> a&gt;b;</span><br><span class="line">&#125;</span><br><span class="line"><span class="built_in">sort</span>(a, a+<span class="number">10</span>, compare);<span class="comment">//在这里不需要对compare函数传入参数</span></span><br></pre></td></tr></table></figure>

<p>（4）若是自己定义的结构体，则比较函数一定要自己写</p>
<h4 id="题解方法一：空间换时间hash-map或map"><a href="#题解方法一：空间换时间hash-map或map" class="headerlink" title="题解方法一：空间换时间hash_map或map"></a>题解方法一：空间换时间hash_map或map</h4><p>今天想查一下c++ hashmap的使用方法，搜出来的一些文章实在辣眼睛，竟然很多都混淆了c++中map和hashmap的区别。</p>
<p>首先，c++ 标准库的std::map内部是排序的，内部使用的是红黑树实现，不管是增加还是查找的时间复杂度 O(logN)O(logN)O(logN)。</p>
<p>而c++ 标准库的hashmap其实叫作std::unordered_map，其增加和查询的时间复杂度才是 O ( 1 ) O(1)O(1)。它提供了类似map的方法。在c++11下直接使用头文件#include <unordered_map> 就可以了，如果不在c++11标准下，也可以使用#include &lt;tr1/unordered_map&gt; 来支持hashmap.<br>————————————————<br>版权声明：本文为CSDN博主「mgsweet」的原创文章，遵循CC 4.0 BY-SA版权协议，转载请附上原文出处链接及本声明。<br>原文链接：<a href="https://blog.csdn.net/mgsweet/article/details/105649792">https://blog.csdn.net/mgsweet/article/details/105649792</a></p>
<p>上文是找到的hash_map和map的区别，map的使用很简单，但是效果比排序后找还差。所以尝试使用hash_map，但是hash_map未加入C++11标准中。</p>
<p>在VC中编译： </p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">1 #include &lt;hash_map&gt;</span><br><span class="line">2 using namespace stdext;</span><br><span class="line">3 hash_map&lt;int ,int&gt; myhash;</span><br></pre></td></tr></table></figure>

<p> 在GCC中编译：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">1 #include &lt;ext/hash_map&gt;</span><br><span class="line">2 using namespace __gnu_cxx;</span><br><span class="line">3 hash_map&lt;int ,int&gt; myhash;</span><br></pre></td></tr></table></figure>

<p>所以使用unordered_map（与map用法一致）</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;unordered_map&gt;</span></span></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Solution</span> &#123;</span></span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    <span class="function"><span class="keyword">int</span> <span class="title">findRepeatNumber</span><span class="params">(vector&lt;<span class="keyword">int</span>&gt;&amp; nums)</span> </span>&#123;</span><br><span class="line">        unordered_map&lt;<span class="keyword">int</span>, <span class="keyword">int</span>&gt; new_nums;</span><br><span class="line">        <span class="keyword">for</span> (<span class="keyword">int</span> i = <span class="number">0</span>; i &lt; (<span class="keyword">int</span>)nums.<span class="built_in">size</span>(); i++) &#123;</span><br><span class="line">            <span class="keyword">if</span>(new_nums[nums[i]] == <span class="number">1</span>)&#123;</span><br><span class="line">                <span class="keyword">return</span> nums[i];</span><br><span class="line">            &#125;</span><br><span class="line">            new_nums[nums[i]] = <span class="number">1</span>;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">return</span> <span class="number">-1</span>;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>

<p>发现也没有自己的解法高</p>
<p><img src="https://cdn.jsdelivr.net/gh/RDXSun/img_warehouse/img/20210806170727.png"></p>
<h4 id="题解方法二（最牛的方法）：原地置换法（索引值与值若无重复应该一一对应）"><a href="#题解方法二（最牛的方法）：原地置换法（索引值与值若无重复应该一一对应）" class="headerlink" title="题解方法二（最牛的方法）：原地置换法（索引值与值若无重复应该一一对应）"></a>题解方法二（最牛的方法）：原地置换法（索引值与值若无重复应该一一对应）</h4><p>题目说明尚未被充分使用，即 在一个长度为 n 的数组 nums 里的所有数字都在 0 ~ n-1 的范围内 。 此说明含义：数组元素的 索引 和 值 是 一对多 的关系。<br>因此，可遍历数组并通过交换操作，使元素的 索引 与 值 一一对应（即 nums[i] = inums[i]=i ）。因而，就能通过索引映射对应的值，起到与字典等价的作用。</p>
<p>​    遍历中，第一次遇到数字 xx 时，将其交换至索引 xx 处；而当第二次遇到数字 xx 时，一定有 nums[x] = xnums[x]=x ，此时即可得到一组重复数字。</p>
<p><strong>算法流程：</strong></p>
<ol>
<li>遍历数组 numsnums ，设索引初始值为 i = 0i=0 :<ol>
<li>若 nums[i] =i ： 说明此数字已在对应索引位置，无需交换，因此跳过；</li>
<li>若 nums[nums[i]] = nums[i] ： 代表索引 nums[i] 处和索引 i 处的元素值都为 nums[i] ，即找到一组重复值，返回此值 nums[i] ；</li>
<li>否则： 交换索引为 i 和 nums[i] 的元素值，将此数字交换至对应索引位置。</li>
</ol>
</li>
<li>若遍历完毕尚未返回，则返回 -1 。</li>
</ol>
<p>复杂度分析：<br>时间复杂度 O(N)O(N) ： 遍历数组使用 O(N)O(N) ，每轮遍历的判断和交换操作使用 O(1)O(1) 。<br>空间复杂度 O(1)O(1) ： 使用常数复杂度的额外空间。</p>
<p>作者：jyd<br>链接：<a href="https://leetcode-cn.com/problems/shu-zu-zhong-zhong-fu-de-shu-zi-lcof/solution/mian-shi-ti-03-shu-zu-zhong-zhong-fu-de-shu-zi-yua/">https://leetcode-cn.com/problems/shu-zu-zhong-zhong-fu-de-shu-zi-lcof/solution/mian-shi-ti-03-shu-zu-zhong-zhong-fu-de-shu-zi-yua/</a><br>来源：力扣（LeetCode）<br>著作权归作者所有。商业转载请联系作者获得授权，非商业转载请注明出处。</p>
<p>由此写出代码如下：</p>
<p><img src="https://cdn.jsdelivr.net/gh/RDXSun/img_warehouse/img/20210806171947.png"></p>
<p>多次提交发现执行内速度差那么几ms就会差很多，做不到beat100%。</p>
]]></content>
      <categories>
        <category>leetcode</category>
        <category>算法</category>
        <category>原地置换</category>
      </categories>
      <tags>
        <tag>Cpp</tag>
        <tag>sort排序</tag>
        <tag>哈希表</tag>
        <tag>原地置换</tag>
      </tags>
  </entry>
  <entry>
    <title>侯捷C++面向对象笔记（一）不带指针类的部分</title>
    <url>/2021/08/07/%E4%BE%AF%E6%8D%B7C-%E9%9D%A2%E5%90%91%E5%AF%B9%E8%B1%A1%E7%AC%94%E8%AE%B0/</url>
    <content><![CDATA[<p><strong>介绍：</strong>侯捷老师面向对象高级编程的笔记</p>
<span id="more"></span>

<h1 id="学习目标"><a href="#学习目标" class="headerlink" title="学习目标"></a>学习目标</h1><ul>
<li>以良好的方式编写C++ class （基于对象）</li>
<li>学习classes之间的关系（面向对象）</li>
</ul>
<h2 id="C-演化"><a href="#C-演化" class="headerlink" title="C++演化"></a>C++演化</h2><ul>
<li>C++98（本门课主要内容）</li>
<li>C++03</li>
<li>C++11</li>
<li>C++14</li>
</ul>
<h2 id="C-包含"><a href="#C-包含" class="headerlink" title="C++包含"></a>C++包含</h2><ul>
<li>C++语言</li>
<li>C++标准库（使用标准库非常重要）</li>
</ul>
<h1 id="需要看的书籍"><a href="#需要看的书籍" class="headerlink" title="需要看的书籍"></a>需要看的书籍</h1><p>《C++ primer》</p>
<p>《Effective C++》</p>
<p>《The C++ STANDARD LIBRARY》</p>
<p>《STL源码剖析》</p>
<h1 id="笔记"><a href="#笔记" class="headerlink" title="笔记"></a>笔记</h1><h3 id="零-C-与C的区别以及C-的数据和函数"><a href="#零-C-与C的区别以及C-的数据和函数" class="headerlink" title="零 C++与C的区别以及C++的数据和函数"></a>零 C++与C的区别以及C++的数据和函数</h3><p>C++的struct几乎等同于Class</p>
<img src="https://cdn.jsdelivr.net/gh/RDXSun/img_warehouse/img/20210807102745.png" style="zoom:50%;" />

<h3 id="一-头文件与类的声明"><a href="#一-头文件与类的声明" class="headerlink" title="一 头文件与类的声明"></a>一 头文件与类的声明</h3><img src="https://cdn.jsdelivr.net/gh/RDXSun/img_warehouse/img/20210807103109.png" style="zoom: 50%;" />

<p>标准库用&lt;&gt;，自己声明的库为” “</p>
<p>关于头文件标准库的写法</p>
<img src="https://cdn.jsdelivr.net/gh/RDXSun/img_warehouse/img/20210807103337.png" style="zoom:50%;" />

<p>把cout想象成一个屏幕，那“i=”这些就是往cout（屏幕）丢‘</p>
<h4 id="头文件的正规写法"><a href="#头文件的正规写法" class="headerlink" title="头文件的正规写法"></a>头文件的正规写法</h4><p>防卫式声明（guard）</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="meta-keyword">ifndef</span> __COMPLEX__  <span class="comment">//if not define 这个变量，那么就define它；如果define了，就不会重复进行</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">define</span> __COMPLES__</span></span><br><span class="line"></span><br><span class="line"></span><br><span class="line">...</span><br><span class="line">    </span><br><span class="line">    </span><br><span class="line"><span class="meta">#<span class="meta-keyword">endif</span></span></span><br></pre></td></tr></table></figure>

<p>编译器对头文件进行以下预处理：</p>
<ol>
<li> 编译器将处理掉所有注释，以空格代替；</li>
<li> 删除#define，展开所有宏定义；</li>
<li> 处理条件编译指令#if、#ifdef、#elif、#else、#endif；</li>
<li> 处理#include，展开被包含的头文件（直接将头文件复制进文件）</li>
<li> 保留编译器需要使用的#progma指令等等。</li>
</ol>
<p>从第四点可知，如果不对头文件进行防卫式声明，那么可能会对某一个头文件进行多次处理，势必会产生重复定义和声明，也会让文件的大小变得很庞大（因为多次将头文件内容进行复制）。</p>
<h4 id="头文件的布局"><a href="#头文件的布局" class="headerlink" title="头文件的布局"></a>头文件的布局</h4><img src="C:/Users/97660/AppData/Roaming/Typora/typora-user-images/image-20210807112506731.png" alt="image-20210807112506731" style="zoom: 50%;" />

<h4 id="class的声明"><a href="#class的声明" class="headerlink" title="class的声明"></a>class的声明</h4><p>函数可以在声明内部定义，也可以在body之外定义</p>
<img src="C:/Users/97660/AppData/Roaming/Typora/typora-user-images/image-20210807112632635.png" alt="image-20210807112632635" style="zoom: 50%;" />

<p><strong>如果要设计另外一个复数，里面的re和im不是double，是float的，那应该怎么办？多定义一个class又麻烦</strong></p>
<h4 id="使用模板class-template"><a href="#使用模板class-template" class="headerlink" title="使用模板class template"></a>使用模板class template</h4><img src="https://cdn.jsdelivr.net/gh/RDXSun/img_warehouse/img/20210807112822.png" style="zoom:67%;" />

<p>上面是声明，下面是使用</p>
<hr>
<h3 id="二-inline（内联）函数"><a href="#二-inline（内联）函数" class="headerlink" title="二 inline（内联）函数"></a>二 inline（内联）函数</h3><p>像上图，real和imag函数在函数内部定义完成，成为<strong>inline候选人</strong>。<strong>如果函数是一个inline function，会比较快而且好。</strong>但是有些function就算写成inline，编译器也没有能力把它做成inline，分界点就在于：函数如果太复杂就没办法了。（<strong>是不是inline由编译器来决定，程序员是不知道的，只能通过复杂或简单来主观判断</strong>）</p>
<p>如果不在body内定义，想要做成inline的话，可以如下写：</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">inline</span> <span class="keyword">double</span></span></span><br><span class="line"><span class="function"><span class="title">image</span><span class="params">(<span class="keyword">const</span> complex &amp;x)</span></span>&#123;</span><br><span class="line">    <span class="keyword">return</span> x.<span class="built_in">imag</span>();</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>



<hr>
<h3 id="三-access-level（访问级别）"><a href="#三-access-level（访问级别）" class="headerlink" title="三 access level（访问级别）"></a>三 access level（访问级别）</h3><p><strong>public、private和protected</strong></p>
<p>C++的数据应该private（只给自己用），而函数一般public，也有自己用的是private。而public和private并不是一定要分成两段，可以写多个。</p>
<img src="https://cdn.jsdelivr.net/gh/RDXSun/img_warehouse/img/20210807114114.png" style="zoom:80%;" />

<p>如上如所示，re和im是private的，所以不能直接通过对象来拿他的数据，但是可以通过函数real和imag来拿。</p>
<hr>
<h3 id="四-构造函数"><a href="#四-构造函数" class="headerlink" title="四 构造函数"></a>四 构造函数</h3><p>构造函数不需要有返回类型，因为就是要创建对象的</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">complex</span>&#123;</span></span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">	<span class="built_in">complex</span>(<span class="keyword">double</span> r = <span class="number">0</span>, <span class="keyword">double</span> i = <span class="number">0</span> )&#123; <span class="comment">//默认参数，default argument，若是初始化不添加参数则为默认值</span></span><br><span class="line">        re = r;</span><br><span class="line">        im = i;</span><br><span class="line">    &#125;</span><br><span class="line"><span class="keyword">private</span>:</span><br><span class="line">    <span class="keyword">double</span> re, im;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>只有构造函数才有的写法，大气的特殊写法：<strong>初值列，初始列</strong>，如下：</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">complex</span>&#123;</span></span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">	<span class="built_in">complex</span>(<span class="keyword">double</span> r = <span class="number">0</span>, <span class="keyword">double</span> i = <span class="number">0</span> )</span><br><span class="line">    : <span class="built_in">re</span>(r), <span class="built_in">im</span>(i) <span class="comment">//这样相当于赋值，但是最准确的是设初值，</span></span><br><span class="line">    &#123; &#125;</span><br><span class="line">   	</span><br><span class="line"><span class="keyword">private</span>:</span><br><span class="line">    <span class="keyword">double</span> re, im;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>这样书写效率比上面那种直接赋值的要高很多，最后结果是一样的。这两种的区别简单讲起来就是一个变量的值来源于两个阶段：初始化和赋值，上面的初始列就相当于设置初值。如果不在冒号后面做而是在大括号里做，就相当于放弃了初始化这件事，效率差（<strong>这里有问题：为什么效率差了一些</strong>）。</p>
<p>冒号后面能做很多事，最常看见的就是初始化。</p>
<p><strong>对象的创建</strong>有多种方法</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">complex c1(2,1);</span><br><span class="line">complex c2; //没有赋值则是默认值</span><br><span class="line">complex* p = new complex(4);</span><br></pre></td></tr></table></figure>

<p>在程序里不能直接调用构造函数，只能从对象的创建时顺带调用。</p>
<p>与构造函数相反的是，析构函数，<strong>class有两种经典分类：带指针和不带指针，而上述截图的就是不带指针的，不带指针的类多半不用写析构函数，因为不需要释放内存。</strong></p>
<h4 id="重载"><a href="#重载" class="headerlink" title="重载"></a>重载</h4><p>构造函数可以有很多个——overloading（重载）。就算函数名字相同，编译后的实际名称是不同的（所以在编译器是不会有名字相同的函数的），如下：</p>
<p><img src="https://cdn.jsdelivr.net/gh/RDXSun/img_warehouse/img/20210807201626.png"></p>
<p>两个real函数在编译后的实际名称不同。</p>
<p>在上述代码中要注意，两个complex构造函数能不能这样写呢？<strong>不行！</strong>因为第一个complex构造函数<strong>有默认值</strong>，比如如果创建对象<code>complex c1;</code>不给参数，两个构造函数都能调用，所以这时候就不允许2这么写。</p>
<h4 id="把构造函数放入private区"><a href="#把构造函数放入private区" class="headerlink" title="把构造函数放入private区"></a>把构造函数放入private区</h4><p>这样子就不能创建对象，但<strong>还是会有人将构造函数放入private区</strong>中。</p>
<p>如下：</p>
<img src="https://cdn.jsdelivr.net/gh/RDXSun/img_warehouse/img/20210807203331.png" style="zoom:80%;" />

<p>class A的实例只有一份，不允许别人创建，而那一份需要通过getInstance来取得。这种写法<strong>叫Singleton</strong></p>
<hr>
<h3 id="五-常量成员函数"><a href="#五-常量成员函数" class="headerlink" title="五 常量成员函数"></a>五 常量成员函数</h3><p>const member functions，如下</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">complex</span>&#123;</span></span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">	<span class="function"><span class="keyword">double</span> <span class="title">real</span> <span class="params">()</span> <span class="keyword">const</span> </span>&#123;<span class="keyword">return</span> re;&#125;</span><br><span class="line">	<span class="function"><span class="keyword">double</span> <span class="title">imag</span> <span class="params">()</span> <span class="keyword">const</span> </span>&#123;<span class="keyword">return</span> im;&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">private</span>:</span><br><span class="line">	<span class="keyword">double</span> re, im;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>在class里的函数会分为：<strong>会改变private数据和不会改变数据的函数</strong>。上述就是不会改变数据内容的，所以声明时<strong>一定要加const</strong>，<strong>不加会有什么后果？</strong></p>
<p>如下：</p>
<p><img src="https://cdn.jsdelivr.net/gh/RDXSun/img_warehouse/img/20210807204228.png"></p>
<p>对于右边的对象实例化，由于用const声明，所以c1是一个不可改变的对象。但是如果read和imag没有加const的话，就说明这个对象可能会被改变，<strong>这与对象的不可修改性矛盾</strong>，所以不让其调用这个函数。</p>
<hr>
<h3 id="六-参数传递：pass-by-value-vs-pass-by-reference-to-const"><a href="#六-参数传递：pass-by-value-vs-pass-by-reference-to-const" class="headerlink" title="六 参数传递：pass by value vs. pass by reference(to const)"></a>六 参数传递：pass by value vs. pass by reference(to const)</h3><p>pass by value就是把整个变量都压入栈中，若是这个变量超级大，那就不好。</p>
<p>所以如果传引用（pass by reference）的话，就是传这个变量在内存中的地址，传引用就相当于传指针，所以<strong>最好都是传引用</strong>。</p>
<p>但是传引用在函数内部修改会影响函数外部，所以出现了<strong>pass by reference to const</strong>，这样如果函数内部修改引用处内容<strong>就会出错</strong>，如下就是参数传递的三种形式：</p>
<p><img src="https://cdn.jsdelivr.net/gh/RDXSun/img_warehouse/img/20210807205121.png"></p>
<h3 id="七-返回值传递：return-by-value-vs-return-by-reference（to-const）"><a href="#七-返回值传递：return-by-value-vs-return-by-reference（to-const）" class="headerlink" title="七 返回值传递：return by value vs. return by reference（to const）"></a>七 返回值传递：return by value vs. return by reference（to const）</h3><p>下图是一些用法，有&amp;就是by reference</p>
<p><img src="https://cdn.jsdelivr.net/gh/RDXSun/img_warehouse/img/20210807205729.png"></p>
<p>虽然这个点很小，但是<strong>会影响程序运行的效率</strong>。返回值也<strong>尽量by reference</strong></p>
<p><strong>但也有时候不可以，这个问题留待后讲</strong></p>
<h4 id="什么时候不能return-by-reference"><a href="#什么时候不能return-by-reference" class="headerlink" title="什么时候不能return by reference"></a>什么时候不能return by reference</h4><p>函数返回值有两种情况：</p>
<p>① 必须新建一个变量来存储返回值。</p>
<p>② 可以将返回值存储至传进来的参数里面。</p>
<p>很明显第一种情况不能传引用，因为这个变量所在的内存在函数结束后就被释放了，所以第一种情况就不能传引用。如下是一个可传引用的例子：</p>
<p><img src="https://cdn.jsdelivr.net/gh/RDXSun/img_warehouse/img/20210808123311.png"></p>
<h4 id="语法分析"><a href="#语法分析" class="headerlink" title="语法分析"></a>语法分析</h4><p><img src="https://cdn.jsdelivr.net/gh/RDXSun/img_warehouse/img/20210808124625.png"></p>
<p>上图代码，定义的函数是&amp;传引用的，但是return确实指针所指向的东西，那这样对吗？<strong>对的！</strong></p>
<p>虽然我返回的是value，但是<strong>传递者无需知道接收者以reference形式接收</strong>。</p>
<p>比如上面的*ths就是传递者，而complex就是接收端。</p>
<p>再比如<img src="https://cdn.jsdelivr.net/gh/RDXSun/img_warehouse/img/20210808124948.png"></p>
<p>c1是传递者，const complex &amp;r是接收者，那c1也不需要管传的是value还是reference，这就是reference的好处。</p>
<p>**连串赋值c3+=c2+=c1;**分析</p>
<p>先是c1加到c2，c2再加到c3。但是上面重载时，如果返回类型是void，那c2就无法加到c3，所以对于操作符重载的时候要考虑连串赋值的问题。</p>
<hr>
<h3 id="八-friend-友元"><a href="#八-friend-友元" class="headerlink" title="八 friend 友元"></a>八 friend 友元</h3><p>类比于日常身份的朋友，日常的朋友可以来拿钱，而类比后就是拿数据。</p>
<p>比如上面的friend声明了__doapl是complex的朋友，于是就可以直接拿数据，如下直接就可以引用complex的数据：</p>
<p><img src="https://cdn.jsdelivr.net/gh/RDXSun/img_warehouse/img/20210807213842.png"></p>
<p>如果通过函数来拿就会慢一点，用friend就会快。朋友不能太多，因为friend是打开了封装，而C++就是封装的。</p>
<h4 id="一个很重要的问题：相同class的各个objects互为friends"><a href="#一个很重要的问题：相同class的各个objects互为friends" class="headerlink" title="一个很重要的问题：相同class的各个objects互为friends"></a>一个很重要的问题：相同class的各个objects互为friends</h4><p>所以以下的行为（直接调用另一个对象的数据）看似打破了封装，但是也是成立的。</p>
<img src="https://cdn.jsdelivr.net/gh/RDXSun/img_warehouse/img/20210808120159.png" style="zoom:67%;" />



<hr>
<h3 id="总结-设计一个类注意的地方"><a href="#总结-设计一个类注意的地方" class="headerlink" title="总结 设计一个类注意的地方"></a>总结 设计一个类注意的地方</h3><p>1、数据一定放在private里</p>
<p>2、参数尽可能的以reference来传</p>
<p>3、返回值尽可能以reference来传</p>
<p>4、在类里面的函数要加一定要加const</p>
<p>5、构造函数尽量用初始列</p>
<hr>
<h3 id="九-操作符重载"><a href="#九-操作符重载" class="headerlink" title="九 操作符重载"></a>九 操作符重载</h3><p>是C++很重要的特性，在C++里面，操作符（比如+、-等）相当于函数，可以对操作符进行重载。</p>
<h4 id="操作符重载一：成员函数"><a href="#操作符重载一：成员函数" class="headerlink" title="操作符重载一：成员函数"></a>操作符重载一：成员函数</h4><p>+=是二元操作符，如c2+=c1，所有的操作符只要有左右的，都是作用到左边，而调用操作符函数的规则也一样。</p>
<p><img src="https://cdn.jsdelivr.net/gh/RDXSun/img_warehouse/img/20210808123856.png"></p>
<p>图中两个函数是一样的，this是所有成员函数都会带有的隐藏变量（<strong>写代码不能写也不必写出来</strong>），调用者就是this，比如c2+=c1，c2调用函数，则c2就是this，this是一个指针。</p>
<h4 id="操作符重载二：非成员函数（无this）"><a href="#操作符重载二：非成员函数（无this）" class="headerlink" title="操作符重载二：非成员函数（无this）"></a>操作符重载二：非成员函数（无this）</h4><p>如下是虚数加法的三种写法：</p>
<p><img src="https://cdn.jsdelivr.net/gh/RDXSun/img_warehouse/img/20210808155703.png"></p>
<h5 id="问题：为什么上面不用reference而是value？"><a href="#问题：为什么上面不用reference而是value？" class="headerlink" title="问题：为什么上面不用reference而是value？"></a>问题：为什么上面不用reference而是value？</h5><p>上面的函数绝不可以return by reference，因为它们返回必定是local object，因为加好之后肯定是需要在外面创个东西来存储。</p>
<h5 id="typename直接加小括号是创建“临时对象”（temp-object）"><a href="#typename直接加小括号是创建“临时对象”（temp-object）" class="headerlink" title="typename直接加小括号是创建“临时对象”（temp object）"></a>typename直接加小括号是创建“临时对象”（temp object）</h5><p>该临时对象在下一行就消失了</p>
<p><img src="https://cdn.jsdelivr.net/gh/RDXSun/img_warehouse/img/20210809162544.png"></p>
<p><img src="C:/Users/97660/AppData/Roaming/Typora/typora-user-images/image-20210809162618745.png" alt="image-20210809162618745"></p>
<p>上面最后三个也是临时变量，只有一行的生命周期。</p>
<p><strong>对于特殊操作符&lt;&lt;，只能选择全局函数</strong>，因为cout是早就有的，不可能认识新的类型？（没理解）</p>
<p><img src="C:/Users/97660/AppData/Roaming/Typora/typora-user-images/image-20210809163613689.png" alt="image-20210809163613689"></p>
<p>上面这个函数不能用const来修饰，虽然看起来没有更改，但是在输出至os的时候，会改变os的状态。（ostream本身决定的）</p>
<p>虽然没有人关心输出的是什么，可以用void，但是如果连串输出的时候不能是void，所以最后还要用ostream。</p>
<p>因为os不是函数内部创建的，所以可以return by reference</p>
<hr>
<h3 id="十-class-body之外的各种定义"><a href="#十-class-body之外的各种定义" class="headerlink" title="十 class body之外的各种定义"></a>十 class body之外的各种定义</h3><p>class body外的函数分为两种函数：成员函数或全局函数</p>
]]></content>
      <categories>
        <category>cpp基础</category>
      </categories>
      <tags>
        <tag>Cpp基础</tag>
        <tag>侯捷课程</tag>
      </tags>
  </entry>
  <entry>
    <title>留存纪念的介绍</title>
    <url>/2021/08/05/hello-world/</url>
    <content><![CDATA[<p>系统生成介绍，忽略就好</p>
<span id="more"></span>

<p>Welcome to <a href="https://hexo.io/">Hexo</a>! This is your very first post. Check <a href="https://hexo.io/docs/">documentation</a> for more info. If you get any problems when using Hexo, you can find the answer in <a href="https://hexo.io/docs/troubleshooting.html">troubleshooting</a> or you can ask me on <a href="https://github.com/hexojs/hexo/issues">GitHub</a>.</p>
<h2 id="Quick-Start"><a href="#Quick-Start" class="headerlink" title="Quick Start"></a>Quick Start</h2><h3 id="Create-a-new-post"><a href="#Create-a-new-post" class="headerlink" title="Create a new post"></a>Create a new post</h3><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">$ hexo new <span class="string">&quot;My New Post&quot;</span></span><br></pre></td></tr></table></figure>

<p>More info: <a href="https://hexo.io/docs/writing.html">Writing</a></p>
<h3 id="Run-server"><a href="#Run-server" class="headerlink" title="Run server"></a>Run server</h3><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">$ hexo server</span><br></pre></td></tr></table></figure>

<p>More info: <a href="https://hexo.io/docs/server.html">Server</a></p>
<h3 id="Generate-static-files"><a href="#Generate-static-files" class="headerlink" title="Generate static files"></a>Generate static files</h3><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">$ hexo generate</span><br></pre></td></tr></table></figure>

<p>More info: <a href="https://hexo.io/docs/generating.html">Generating</a></p>
<h3 id="Deploy-to-remote-sites"><a href="#Deploy-to-remote-sites" class="headerlink" title="Deploy to remote sites"></a>Deploy to remote sites</h3><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">$ hexo deploy</span><br></pre></td></tr></table></figure>

<p>More info: <a href="https://hexo.io/docs/one-command-deployment.html">Deployment</a></p>
]]></content>
  </entry>
</search>
